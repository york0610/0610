/*
 * This is a Tailwind preset for use by UI extensions.
 */

import { type Config } from "tailwindcss";

const preset: Omit<Config, "content"> = {
  presets: [require("@netlify/tailwind-config")],
};

export default preset;
